import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  heading: {
    fontWeight: "bold",
    fontSize: 40,
    marginBottom: 24,
  },
  container: {
    flex: 1,
    marginTop: 24,
    marginHorizontal: 16,
  },
});
